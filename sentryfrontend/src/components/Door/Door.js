import React from "react";

const Door = () => {
  /**Device Data */
  const deviceId = "";
  const deviceName = "";
  const status = true;
  const isHacked = true;
  const setAlarm = () => {
    /**Function that calls server/database to switch alarm between t/f
     * Returns Boolean
     *
     */
  };
  const setTimer = () => {
    //**Returns Boolean */
  };

  return <></>;
};

export default Door;

//**Timer https://github.com/do-community/react-hooks-timer/issues/1 */
